# Copy this to .env and fill in your values.
# When using the Vite dev server with its built-in proxy, VITE_API_URL should be empty
# so that requests go to the same origin (localhost:5173) and the proxy forwards them.
# For production builds set this to your actual backend URL.
VITE_API_URL=
